# Database
DATABASE_URL="file:./dev.db"

# HTTPS Certificate Configuration (PDF Requirement)
# App will crash if these are missing, invalid, or password is incorrect
SSL_CERT_PATH="./certs/localhost-cert.pem"
SSL_KEY_PATH="./certs/localhost-key.pem"
SSL_CERT_PASSWORD="sikkerhedsapp2024"

# Email Hashing Pepper (Server-side secret - NEVER stored in DB)
# This is a global secret used with per-user salt for email hashing
EMAIL_PEPPER="kryptografi-assignment-super-secret-pepper-2024"
EMAIL_HASH_ITERATIONS=100000
